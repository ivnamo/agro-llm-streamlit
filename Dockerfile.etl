FROM python:3.11-slim

WORKDIR /app

# 1) Instalar dependencias de backend
COPY requirements.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# 2) Copiar TODO el código del repo
COPY . .

# 3) Movernos a la carpeta del servicio ETL
WORKDIR /app/cloud_run/etl_cesens

# 4) Lanzar el servidor HTTP de Functions Framework
CMD ["functions-framework", "--target=backfill_cesens", "--port=8080"]
